# 05 — UI: second "Export XLSX" button beside relabeled "Export CSV"
type: build
status: merged
blocked_by: 04
charter_refs: §Scope-In(second Export XLSX button beside relabeled Export CSV; reuse busy/error status), §Silence-defaults(download tracklist.xlsx + MIME; Sheet1; shared busy/error pattern, one-field-vs-per-format = Type-2; terse labels; aria as precedented), §Priorities 1 (zero regressions to the shipped CSV export/table)
seams: `Toolbar` props (export controls); `App` export orchestration (`handleExport*` + status state)
touches: src/App.tsx, src/components/Toolbar.tsx, src/App.export.test.tsx, src/components/Toolbar.test.tsx, src/styles.css (only if a second button needs it)
attempts: 0/3
split_generation: 0

## What to build
Wire the async `exportXlsx` (#04) to a new download trigger, matching the shipped CSV UX exactly:
- In `Toolbar`: a second button **"Export XLSX"** beside the existing export button, which is relabeled **"Export CSV"** (the only change to the shipped control). Both reuse the existing busy (`aria-busy`) + inline error (`role="status"`, `aria-live="polite"`) pattern.
- In `App`: an `handleExportXlsx` mirroring `handleExport` — call `exportXlsx(sorted, columns)`, wrap the returned bytes in a `Blob` (MIME `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`), download as **`tracklist.xlsx`** via the existing object-URL/anchor/revoke pattern; on failure surface the error state.
- Export status shape (one shared field vs per-format) is a charter **Type-2** call. **The current single shared `exportStatus` tri-state field MUST be rejected** (skeptic F3): today `busy={exportStatus === 'exporting'}` drives the one Export button; a single shared field cannot isolate which control is busy, so reusing it verbatim would flash the shipped **CSV** button busy during an XLSX export — a Priority-1 regression to a shipped control. Resolve to per-control/per-format status (smallest reversible option that still satisfies Priority 1); log the D-entry in this Work log.
- No change to any existing table behavior (sort/filter/page/column-pick/prefs) — zero-regression is Priority 1.

## Acceptance (final — TICKETS s05, from spec.md R5 + skeptic fixes)
- run: `npx vitest run src/App.export src/components/Toolbar`   expect: exit 0 — clicking "Export XLSX" invokes the xlsx path and triggers a `tracklist.xlsx` download with the spreadsheetml MIME; "Export CSV" still triggers the unchanged `tracklist.csv` download; busy/error assert on the correct control.
- **Cross-control busy isolation (skeptic F3):** a test asserts that clicking "Export XLSX" leaves the "Export CSV" button WITHOUT `aria-busy` (and vice-versa) — the shipped CSV control never flashes busy for an XLSX export.
- run: `npm test`   expect: exit 0 — CSV/table **behavior** unchanged; the label-text queries in the existing Toolbar/App tests are updated (see note below), which is not a behavior regression.
- run: `npm run typecheck`   expect: exit 0
- run: `npm run build` then `du -sk dist/`   expect: exit 0, then ≤ 240 (skeptic F6: this is the sole ticket that pulls the new ZIP+OOXML pipeline into the bundle — DCE keeps #02–#04 out of `dist/` until this wires `handleExportXlsx`; the bundle ratchet is enforced here at INTEGRATE, not in CI, because `ci.yml` is charter No-touch except the one smoke step).

## Test-update scope (skeptic F2)
The existing `src/components/Toolbar.test.tsx` and `src/App.export.test.tsx` query the current button by label — `getByRole('button', { name: 'Export' })` (exact, now matches nothing) and `getByRole('button', { name: /Export/ })` (regex, now matches BOTH buttons → throws "multiple elements"). These queries MUST be narrowed to exact `'Export CSV'` / `'Export XLSX'` — a naive `s/Export/Export CSV/` is insufficient for the regex cases. This is a label/query update, NOT a CSV behavior change (Priority 1 holds).

## Work log
- 2026-07-30T04:19Z s02 (MAP): node created. Blocked by #04. Touches App + Toolbar (~2 modules) — at the sizing edge; keep the diff tight or split at TICKETS.
- 2026-07-30T06:15Z s05 (TICKETS): acceptance FINALIZED from spec R5 + three mergeability-skeptic fixes. **F3** — added cross-control busy-isolation acceptance + hardened the Type-2 to REJECT the single shared `exportStatus` default (verified against main: `busy={exportStatus === 'exporting'}` on the lone Export button; a shared tri-state cannot isolate → Priority-1 trap). **F6** — added `build` + `du ≤ 240` (this is the sole bundle-entry point; DCE keeps #02–#04 out of dist). **F2** — added an explicit test-update-scope section for the `/Export/` regex ambiguity; corrected the "tests unchanged" wording to "CSV behavior unchanged; label queries updated". Sizing: 2 modules + 2 test files, tight diff (one added button, one relabel, one handler, status-shape change) → one session; kept as one, not split.
- 2026-07-30T10:16Z s09 (BUILD wave 4, solo): built via worker TDD in isolated worktree cut from origin/main (f926f62). 11 red→green cycles (4 Toolbar + 7 App), each new test proven red first (evidence: "Export CSV"/"Export XLSX" not-found before the relabel+add; onExportXlsx undefined → 0 calls before wiring). Added a second **Export XLSX** button beside the relabeled **Export CSV** control; `handleExportXlsx` mirrors `handleExport` exactly (exportXlsx(sorted, columns) → Blob spreadsheetml MIME → `tracklist.xlsx` via object-URL/anchor/`setTimeout(revoke,0)`-after-click). **D-0003 logged** — resolved the Type-2 status-shape fork by rejecting the single shared `exportStatus` and splitting into independent `csvStatus`/`xlsxStatus` fields threaded through Toolbar props (each own aria-busy button + role=status/aria-live=polite error span, distinguished by aria-label). Existing CSV tests retargeted to exact `'Export CSV'` (and `/Export CSV/` for the busy state where the Spinner's "Loading" label joins the accessible name) — CSV behavior unchanged. Fresh-reviewer two-axis: **Standards CLEAN** (only optional post-green DRY notes: a `downloadBlob` helper + a shared `ExportStatus` alias — both flagged to the arch checkpoint, not blocking; the `as BlobPart` bridge judged acceptable, not `any`), **Spec CLEAN** (CSV control unchanged beyond relabel; MIME/filename/revoke-order correct; shared field genuinely rejected; cross-control isolation asserted in both suites) → 0 fix rounds. INTEGRATE: rebased onto origin/main (no move) → full local gate GREEN: npm test 110/110 (baseline 104, ratchet up), vitest App.export+Toolbar 11/11, typecheck 0, build 0 + du 220≤240, csv smoke rows=10000 (no regression), xlsx smoke rows=10000, no-touch diff = only the 4 Touches files → PR #17 CI `CI` success → squash-merged (main 45f2f09). merged=5. Ticket CLOSED.
