# Decisions — xlsx-export (append-only)

D-#### entries (Type 2 one-liners) and ADR blocks (Type 1) are appended here by
run sessions, newest at the bottom. Format: references/formats.md. Empty at
charter time — every fork so far was pinned in CHARTER.md by the human, so the
ledger is unseeded.

D-0001 | 2026-07-30 | #03 | xlsx string cells use inline strings (t="inlineStr" with <is><t>…</t></is>), not a sharedStrings table | charter §Silence-defaults (smallest reversible; no new dependency) + research note §3 (self-contained per cell, statelessly serializable in the chunked path; sharedStrings remains the reversible upgrade if a future arc adds compression)

D-0002 | 2026-07-30 | #04 | exportXlsx mirrors exportCsv's chunk/yield/options shape as LOCAL copies in xlsx.ts (own `ExportXlsxOptions`, `DEFAULT_CHUNK_SIZE=500`, `yieldToMacrotaskQueue`) rather than importing from csv.ts | charter §No-touch (csv.ts frozen, no import — "mirror the pattern, never import from it") + §Silence-defaults (follow existing convention; smallest reversible). Type 2: reversible, no interface/data-loss risk. NB the async path does the O(rows) build INSIDE the chunk loop (per-chunk buildWorkbook+rowXml) and shares a `packWorkbook` helper with serializeXlsx so bytes stay identical — the literal-but-hollow yield loop was rejected in review as not delivering the charter's "no UI freeze at 10k".

D-0003 | 2026-07-30 | #05 | Export status shape: reject the single shared `exportStatus` tri-state; split into independent per-control `csvStatus` and `xlsxStatus` fields (`'idle'|'exporting'|'error'` each), threaded through Toolbar props with their own `aria-busy` button + `role="status"`/`aria-live="polite"` error span distinguished by aria-label | charter §Silence-defaults (status shape is Type-2; smallest reversible that honors Priority 1) + §Priorities 1 (a single shared field cannot isolate which control is busy → would flash the shipped CSV button busy during an XLSX export = a Priority-1 regression to a shipped control). Type 2: reversible, no interface/data-loss risk beyond the two component props. The TICKETS mergeability skeptic (F3) pre-forced this rejection into #05's acceptance; the cross-control busy-isolation test enforces it.

D-0004 | 2026-07-30 | #05/arch-checkpoint | Wave-4 architecture checkpoint (merged=5): triage all 3 scan findings (F1 duplicated download seam + twin handlers; F2 ExportStatus tri-state repeated; F3 chunked-async driver duplicated csv/xlsx) to Deferred/icebox — no bounded-refactor ticket, no blocking ticket | charter §Scope-Out ("architecture findings — incl. the ExportStatus hoist — go to checkpoint/icebox, never into feature tickets") + §Priorities (all findings are Priority-4 polish; every Done-when already met so none is Blocking; refactoring shipped App handlers pre-FINISH risks Priority-1 regression for zero functional gain; F3 needs no-touch csv.ts/xlsx.ts edits). Deep modules (zip.ts, xlsx-workbook.ts) + testability assessed CLEAN. Type 2: reversible, no code change made. Findings → next arc's charter.
